#pragma once

#include "ies/Common/SmartEnum.hxx"

namespace ies
{

//! @brief Extremum enum to let user can express concept of two extremums: Minimum (Min) or Maximum (Max).
IES_SMART_ENUM(Extremum,
    Min,
    Max
);

}
